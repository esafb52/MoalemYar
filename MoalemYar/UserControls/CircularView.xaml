<UserControl x:Class="MoalemYar.UserControls.CircularView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MoalemYar.UserControls"
             xmlns:controls="clr-namespace:HandyControl.Controls;assembly=HandyControl" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded"
             mc:Ignorable="d" Style="{StaticResource myUserControl}">
    <controls:TransitioningContentControl>
        <controls:ScrollViewer HorizontalScrollBarVisibility="Auto">
            <Grid>

                <StackPanel Orientation="Horizontal" Height="56" VerticalAlignment="Top">
                    <Button x:Name="btnStop" Click="btnStop_Click" Margin="20,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="134" 
                            Style="{StaticResource ButtonInfo}" Height="35">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <Image Name="img" Height="16" Width="16" Margin="10,0,30,0" Source="/Resources/stop.png" />
                            <TextBlock Name="txtStop" TextWrapping="NoWrap" Text="توقف" FontSize="13" Foreground="#ffffff" />
                        </StackPanel>
                    </Button>
                    <TextBlock HorizontalAlignment="Center" Margin="5" Style="{StaticResource TextBlockSubTitle}" Text="دریافت 20 بخشنامه آخر" />
                    <ToggleButton Style="{StaticResource ToggleButtonSwitch}" IsChecked="True" Name="swLimit" Checked="MetroSwitch_Checked" Unchecked="MetroSwitch_Checked" FlowDirection="LeftToRight" Margin="10" />
                </StackPanel>
                <TextBox IsEnabled="False" Name="txtSearch" TextChanged="MetroTextBox_TextChanged" Height="30" VerticalAlignment="Top" Margin="391,10,40,0" />

                <ProgressBar Style="{StaticResource ProgressBarDanger}" Margin="20,56,40,0" Name="prgUpdate" ValueChanged="prgUpdate_ValueChanged" FlowDirection="LeftToRight" VerticalAlignment="Top" />
                <WrapPanel Width="1000" x:Name="waterfallFlow" Margin="20,85,20,10" />
            </Grid>
        </controls:ScrollViewer>
    </controls:TransitioningContentControl>
</UserControl>