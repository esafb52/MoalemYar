<UserControl
    x:Class="MoalemYar.UserControls.ChartTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:chart="clr-namespace:De.TorstenMandelkow.MetroChart;assembly=De.TorstenMandelkow.MetroChart"
    xmlns:controls="clr-namespace:HandyControl.Controls;assembly=HandyControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MoalemYar.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <controls:TransitioningContentControl Margin="10,10,10,0">
        <Border
            Width="300"
            Height="330"
            Background="{Binding ElementName=chart, Path=Background}"
            CornerRadius="5"
            Effect="{StaticResource EffectShadow3}">
            <Grid>
                <chart:ClusteredColumnChart Name="chartColumn" Background="White">
                    <chart:ClusteredColumnChart.Series>
                        <chart:ChartSeries
                            Name="chart"
                            DisplayMember="Caption"
                            ItemsSource="{Binding}"
                            ValueMember="Scores" />
                    </chart:ClusteredColumnChart.Series>
                </chart:ClusteredColumnChart>
            </Grid>
        </Border>
    </controls:TransitioningContentControl>
</UserControl>
